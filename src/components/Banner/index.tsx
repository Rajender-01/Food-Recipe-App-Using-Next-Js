import React from "react";

const index = () => {
  return (
    <div className="relative w-full min-h-[362px] bg-banner d-flex-center">
      <div className="absolute inset-0 bg-black/30"></div>
      <h1 className="relative z-10 text-white max-w-[898px] text-center text-4xl leading-[2.9rem] font-bold">
        Get inspired, cook with passion and enjoy unforgettable moments at the
        table.
      </h1>
    </div>
  );
};

export default index;
